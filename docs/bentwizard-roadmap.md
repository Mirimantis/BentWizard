# BentWizard — Timber Framing Workbench Roadmap (rev 3)

**Name:** BentWizard (retained from the prior iteration; architecture is new)

**Target platform:** FreeCAD 1.1.1
**Layout convention:** Mill Rule — drawings dimension an idealized straight, square timber (reframed July 2026; see Layout rules section. Square Rule remains supported: its reduce-to-ideal step is field work, not model content)
**Pilot project:** Generic test bent

## Governing Rules (amended)

**Tier 1 — Geometry stays native. Unchanged, non-negotiable.**
All solid geometry is built from ordinary FreeCAD objects: Sketches, Part Design features, datums, Assembly joints. Anything the workbench produces must be editable by hand with stock FreeCAD, as if a person had modeled it. This is the lesson of the failed prior attempt.

**Tier 2 — Non-geometric data degrades gracefully.**
Species, grade, joint metadata, check results, and similar data may be workbench-aware. Requirements: (a) with the workbench uninstalled, parts and assemblies remain fully editable — custom properties on native objects satisfy this, since FreeCAD stores them in the file and shows them in the property panel regardless; (b) custom data survives a round trip through a system without the workbench; (c) workbench-gated *functionality* (checks, reports, generators) is acceptable, since behavior — unlike geometry — is regenerable.

**Modeling convention — subtractive joinery.**
Joints are cut the way wood is cut: pockets and holes preferred over pads. Consequences:
- `Length` in a timber's VarSet = the full stick as purchased and cut, tenons and tongues included. Cut lists read it directly.
- Tenons via island pockets: one sketch with an outer removal loop and an inner tenon profile; the pocket leaves the island standing.
- Tenon shoulders land at `Z = TenonLength`, not the origin plane; assembly references use shoulder datums or expressions.

**Datum strategy (manual workflow):** sketch on native origin planes or faces with zero offset where possible; set depth by pocket/pad length, not datum offset; set position along the timber by sketch constraint. Reserve offset datums for genuinely floating planes and verify their resolved placement. (See findings log #10.)

## Phase 0 — Manual Workflow Definition (in progress)

Done: parametric timber template · housed mortise & tenon (MT1, shared VarSet driving both halves) · housed dovetail (DT1).
Remaining:
1. Cleanup + subtractive rework of Tenon_MT1 and Tongue_DT1 (session 5)
2. Pegs + drawbore on the M&T
3. Assemble the test bent (Assembly workbench, datum-to-datum)
4. TechDraw: one sheet per timber + bent elevation
5. Manual cut list in Spreadsheet
6. Final workflow document

## Phase 1 — Joint Library & Application Tool
- Joint templates as ordinary FreeCAD files.
- Apply-Joint tool: reproduces the manual steps — cloning native sketches/features, binding a shared VarSet — with parameters entered as plain numbers, expressions written programmatically.
- Mated-pair logic: one VarSet per joint instance drives both timbers.
- Reference-face aware: joint layout measures from the named reference faces and stick ends. Housings are **optional bearing features** (Mill Rule usage — spreading load across the full beam width instead of the tenon alone) with real designed depths.
- **Landing-face-relative joint definitions** (from findings #9): one joint instance = one VarSet + one landing-frame datum per timber role. Implementation verified in 1.1.1 (Phase 1 start): the frame is a `Part::LocalCoordinateSystem` datum; sketches and datums attach to the frame's own planes and follow it rigidly, so retargeting face/station/end/hand = re-placing one object. Caveat: followers must reference the LCS sub-element, not its child plane object (the latter resolves to identity placement). Template sketches are position-agnostic — constrained only to the joint VarSet and their own timber's Dims — and attach to the landing frame, which the tool places and orients (end, station, hand). Strict linter rules: one VarSet per instance; no multi-instance sketches; no cross-timber dims references; no solid-face references. Joint templates carry non-geometric schedule data as VarSet properties (e.g. PegCount).
- **New timber from template**: atomic duplicate of pristine template + VarSet with verified remap (findings #2, #12). **Duplicate Timbers** (née Duplicate Bent): instance-layer VarSets (Dims, joints) duplicate; group bindings preserved.
- **Two-level structure assembly** (adopted July 2026 after the first GUI shakedown; supersedes the brief bays-as-links design — links could not receive tie-beam joinery and weren't parametric, so every timber is a real body; FabricationSignature returns the deduplication at the Phase-2 list layer). Bents are nested `Assembly::AssemblyObject`s inside a parent frame assembly; assembly is not a second button — **Apply Timber Joint assimilates each joint the moment it's created** (`assimilate_joint`), with **Assemble Timbers** as the bulk/repair/reground path. Container rules, deterministic: both timbers loose → new bent sub-assembly grounded at the anchor timber (the Principal Post — framers measure everything from it); one loose → joins the other's assembly; same assembly → Fixed joint lives there; different assemblies → the joint goes to the deepest common parent, and the first cross-bent joint creates the frame, grounded by the *oldest* bent. Each timber joint = one Fixed assembly joint referencing the JointFrame/MateFrame LCS datums (never solid faces), reference form `(moving_part, ["Body.LCS.", ""])`. **Mate parity**: on faces where the landing frame's Z points into the wood (`FACES` flip_z — faces 2/3), plain frame coincidence seats the mate mirrored through the bearing plane (the shakedown's inverted end-B bent); the engaged pose is coincidence rotated 180° about the frame's local Y, carried in the Fixed joint's `Offset1` so its nominal pose IS the correct seat, and applied in `engagement_placement` (Preview shares it). Pinned by the face 1–4 × end A/B seating matrix (overlap ≈ 0). **No temporary anchors, ever**: grounds are each bent's internal principal timber and the principal bent in the frame — **one red padlock per assembly level**, meaning "the fixed reference *within this assembly*", not "grounded to the world"; only the chain frame → principal bent → Principal Post is world-fixed. Pre-seating moves the side **not yet connected to its assembly's ground** (`isPartConnected`, the new joint suppressed during the test) — a tie already seated on bent 1 stays put and the new bent comes to it. After a container seats, its internal assembly joints are touched (`refresh_joint_display`) so their icons follow — a joint's viewprovider doesn't redraw when only its container moved. An unconnected bent floats user-movable until a tie joint constrains it (verified: the parent solver leaves it untouched), then snaps to its parametric position — a plain recompute re-solves the chain, so tie-beam `Length` drives bay width live. (The Transform tool bypasses the solver by design; a transformed part snaps back on the next solve/recompute.) **Duplicate Timbers** reproduces relative placements and assembles the copies into a new offset bent sub-assembly, ready for tie beams (the 10-step bay sequence is a smoke test). Terminology guard: BentWizard joinery is a "timber joint" in all user-facing text; the `TimberJointVars` Std Group stays clear of every Assembly's own `Joints` group.
- **Per-joint 3D handle** — *done (July 2026)*: a clickable marker at each timber joint for at-a-glance "this intersection is joined" and direct VarSet access. `Handle_J-<Kind>-<serial>` is an `App::FeaturePython` with a `Joint` link to its joint's VarSet and an `App::PropertyLinkGlobal` `Frame` to the anchor's landing frame — both **structural, never label-matched**, so a renamed handle still resolves; a workbench-gated ViewProvider draws crossed amber bars there (billboarded `SoAnnotation` + `SoShapeScale`, as Assembly's padlock). **No App-level Proxy** — verified headless: the object recomputes, saves and reloads clean, and `Document.xml` names no BentWizard module at all, so a workbench-less open is a plain group holding native data with zero geometric effect. Apply Timber Joint creates it, `assimilate_joint` re-files it, Remove Joint takes it (it is not a `joint_members` member — same blind spot as the preview group), and **Assemble Timbers adopts doc-wide**, which is the migration path for older documents (reported in its summary; it now returns a fourth value, `adopted`).
  - **Tree home follows the joint, not the document root** (decided at implementation): the handle holds its joint's VarSet and files in a `TimberJoints_<AssemblyLabel>` Std Group **inside the assembly the joint belongs to** — one folder of joints per bent, one node per joint, the frame's group holding the cross-bent ties. Re-filing respects the user's own arrangement: a VarSet dragged out to sit beside its handle stays there, only one that has drifted out of the folder entirely is re-filed.
  - **An App-side group extension is only half of a group** (GUI testing): the tree draws a group's children from the *view provider*, so `App::GroupExtensionPython` alone left the VarSet un-nested and un-droppable, falling through to the container level even though `handle.Group` held it — invisible to headless tests, which read the App side and passed. The view provider must carry `Gui::ViewProviderGroupExtensionPython` too (BIM, CAM and FEM pair them the same way). Because the marker's Proxy is Transient, that Gui extension is added at runtime with it. "Belongs to" is not a second concept: it is the container of the joint's Fixed assembly joint, i.e. the container rules resolved once by `assimilate_joint`. Verified that nesting a Std Group (and a joint VarSet, which body features reference by expression from sibling assemblies) inside an `Assembly::AssemblyObject` raises no scope complaint and keeps every joint seated — the same result the Dims-VarSet-inside-Body nesting had. Root-level `TimberJointVars`/`Joints` groups migrate in place and are dropped once empty.
  - **The marker is view-only.** The handle owns no Placement, so nothing it does can perturb geometry or the solver; the marker's position is read from the landing frame's global placement and refreshed by one `addDocumentObserver` on `slotRecomputedDocument`. That covers what an object-local update cannot — a station edit, and a whole bent seating in the frame (`refresh_joint_display`'s blind spot).
  - **Nothing about the view is persisted** (revised after GUI testing): the ViewProvider Proxy is marked `Transient`, and `view_joint_handle.install()` attaches markers when the workbench activates, `slotFinishRestoreDocument` when a document opens later. A *saved* proxy printed a `ModuleNotFoundError` traceback per handle on every workbench-less open — Tier 2 in the letter but not the spirit — and would have frozen the module and class names forever. This also covers handles built headless, which never carry a view provider because headless FreeCAD writes no Gui document.
  - **3D picking needs `SoFCSelection`** (found in GUI testing): with a plain `SoSeparator` root the marker drew correctly but no click resolved to it. `SoFCSelection` with `documentName`/`objectName` set is the node that routes a pick to its object; Assembly's joint markers use the same. A single 3D click now selects the handle in the tree.
  - **Interaction is tree-side, by platform limitation** (settled after two GUI rounds): FreeCAD does not dispatch `doubleClicked` or `setupContextMenu` from the **3D view** — `doubleClicked` is documented as "called by the tree", and repeated 3D clicks walk the container hierarchy instead. Upstream issues cover exactly this, including FreeCAD #14701 (*Edit joints by double clicking in 3D view*) filed against **Assembly's own joint markers** — the same class of object — plus #11429 and #12826. So the marker's job is at-a-glance confirmation and selection; the operations live in the tree's double-click/context menu, and the toolbar commands preselect from a marker click (`_pick_joint` maps a selected handle to its joint). If this grates in real use, the escape hatch is an `SoEventCallback` inside the marker's own scene graph, catching double- and right-click when the pick is ours — self-contained, at the cost of stealing events from the viewer. Not built; not worth it until the tree path proves annoying.
  - **The marker is the locus for whole-joint operations** (Adam, at design): a joint is many VarSets, frames, sketches and features across several bodies, and this is the one place it is a single thing. Double-click selects the VarSet (parameters in the Data editor); the context menu is built from `joint_handle.CONTEXT_ACTIONS`, a registry `commands.register()` fills — a future joint-wide tool appends an entry instead of editing the ViewProvider. Currently: Joint parameters, Select joint members, Preview mated joint, Remove timber joint. Deleting the marker never deletes joinery (the VarSet returns to the root group).
  - Owed, deliberately deferred: an advisory `handle-missing` lint rule needs a template-vs-model discriminator first — every `library/` template legitimately has a joint VarSet and no handle.
- **Preview Mated Joint** (findings #7, #11): display both halves engaged for at-a-glance fit verification, independent of body placement.
- Generated sketches use centerline + half-width constraints, not Symmetry (findings #13).
- **Parameter sanity bounds** (inherited from prior iteration): apply-dialog computes min/max from the timbers involved — mortise width ≤ 75% of receiving extent, housing ≤ 50% of through-dimension, dovetail flare clamped to member extent. Linter: strict at severing limits, advisory warning at 35%.
- **User-authored joint templates**: a joint template is an ordinary FreeCAD file — one VarSet (the parameter schema) plus per-role feature stacks. Users author new joints by modeling them manually per project conventions; a "Save as joint template" tool validates (a linter built from the findings-log conventions: origin-plane sketches, single shared VarSet, no solid-face references, half-width constraints, naming, interior islands or removal regions) and registers the file into the library. The apply-joint dialog is generated from the template's VarSet properties — no per-joint code, so adding joints requires no programming. Clone architecture gives free versioning: template edits never affect already-applied joints.
- **Parameter provenance UI**: from any timber or joint, view where its numbers come from — group memberships, binding chains, and overrides; advisory linter reports group deviations.
- **Layered parameter groups**: instance VarSet properties may pass through by expression to group VarSets (type → section → project). Sharing = binding; overriding = replacing one property's expression with a literal; group membership = the binding itself (no hidden state, Tier 2 clean). Tools: "assign selected to group" (bulk expression rewrite), apply-dialog fields accept literal or group binding, advisory linter reports instances deviating from their group.
- **Handed template metadata** (adopted July 2026, testing notes): a Tier 2 `Handed` boolean on the joint template's VarSet. Fully symmetrical joints (defined from the centerline — which the centerline + half-width sketch convention already produces) set it false: Apply Timber Joint hides the hand option, and the linter skips hand-related checks. Phase 2's FabricationSignature already assumes this distinction exists ("handed mirrors hash differently") — making it explicit template metadata now feeds that directly. Same shape as the primary/secondary-roles open item: template self-describes, tools read it, no per-joint code.
- **N-part joint templates / created-part roles** (adopted July 2026, testing notes): support templates with more than two bodies — the wedge in a tusk tenon, the key in a wedged half-dovetail. The template reader is already N-role (per-body feature stacks); the pair assumption lives in engagement/assembly ("the mate-frame carrier is the mover"), which generalizes to one anchor + N movers, each with its own mate frame. The new concept is a **created-part role**: a body with no target timber in `body_map` — the tool clones it wholesale into the document (additive geometry is fine here; subtractive joinery governs cuts *into timbers*, and a wedge is a standalone stick), assembles it to the joint frame, and routes it to the Phase 2 hardware/peg schedule rather than the cut list.
- **Parametrically angled joints** (adopted July 2026, testing notes; sequence after per-joint handle and Save-as-template): joint angle as a VarSet property driving the landing-frame rotation, bounded by an angle range declared as template metadata (dialog clamps, linter enforces). Staged: **(a) vertical angles** — diagonal struts, rafter feet; one rotation in the timber's side plane — then **(b) plan angles** for out-of-square corners. The engagement/assembly layer is largely angle-blind by construction (sketches follow the landing frame rigidly; the MateFrame is a pure frame-coincidence declaration), so the work concentrates in the placement transforms (`FACES`, end semantics, mate parity, seating matrix all currently assume perpendicular), pre-flight bounds, footprint lint, and template authoring (cut profiles valid across the angle range). Angled cuts on *perpendicular* intersections (e.g. a diminished housing) need none of this — see Open Items.
- **Beam tool** (adopted July 2026, testing notes; staged): create an infill timber between two existing timbers — length from the two landing stations plus each end's tenon length (the subtractive `Length` convention makes this direct), joint template applied at both ends, auto-assembled. **It writes an expression, not a number** (see Parametric layout): binding `Length` to the grid rather than freezing a computed literal is what makes the infill track a later span edit, and it is most of that section's tooling work. **Stage 1: dialog infill beam** — select two timbers / landing stations in a dialog; perpendicular joints only; most of the payoff at a fraction of the cost. **Stage 2: the 3-click gesture** — click to place a landing frame on one timber, rubber-band line to a second landing frame, generate the timber at the line's length and angle (Assembly's joint-creation flow and Draft trackers are prior art). Stage 2 wants parametrically angled joints; until then the second click is constrained perpendicular per the template's metadata. Output is entirely native (a normal timber, two normal joints) — only the gesture is new.
- Subtractive convention assessed after Phase 0 assembly: runtime cost neutral (templates are cloned, not computed), template-design cost slightly higher (removal-region decomposition), bookkeeping decisively better (stick-length parameter, tape-measure-true drawing dimensions). Retained.

## Phase 2 — Cut Lists & Drawing Automation
- Cut list is a **procurement document** (per session-10 redesign): timber ID, species, grade, moisture, finish, cut type (boxed heart / FOHC / quartersawn), section, designed length, order length (= designed + trim-allowance parameter), board feet per stick, and order totals. Joinery callouts live on shop drawings / a joinery schedule, not the cut list.
- Specification fields (species, grade, moisture, finish, cut type) are Tier 2 properties on each timber; the generator reads them — the spreadsheet is output, never the source of truth.
- Hardware/peg schedule: generator sums per-joint counts (VarSet properties like PegCount) grouped by diameter; peg length derivable from bore depth + proud allowance.
- Batch TechDraw: projection-group-based dimensioned shop sheets per FabricationSignature (quantity + MemberID list on each; per-timber sheets as an option); frame/bent elevations. Generated sheets set dimension/font styles explicitly, never inheriting user preferences.
- **Positional IDs are display data** (settled at the Phase 1 shakedown, superseding the prior iteration's positional MemberID labels): the permanent identity is the serial Label (`T-Post-003`); position lives in `Position_Tag` (Tier 2, on the Dims VarSet) and is what drawings and cut lists print. Phase 2 may add structured Role/Bent/Position properties composing the tag; renumbering a position never touches a label, an expression, or geometry. Round-trips between model, drawings, and list. Because of that separation, labels themselves went **permissive** (July 2026, testing notes): free-form except the reserved characters `>`, `\`, `;`, and line breaks (strict lint — they break `<<Label>>` expressions or the Placement_Record; verified empirically against 1.1.1), ending in a separator (`-`/`.`/`_`/space) + digit serial that the copy tools bump, preserving the separator. See workflow doc §3.
- **FabricationSignature** (adopted): normalized hash of species, grade, section, stick length, end cut angles, and joint layout in timber-local coordinates. Identical signatures form fabrication groups in the cut list (`GROUP 7 — Qty: 4`). Handed mirrors hash differently (consistent with findings on handed pairs).
- Shop drawings carry reference-face marks and A/B end labels per the adopted face vocabulary (faces 1–4 clockwise from the reference face, viewed from end A). **Face order/labelling is configurable, not fixed** (August 2026 research): a competing SketchUp plugin added an option purely to mimic Dietrich's face order, which says drawing conventions are muscle memory rather than cosmetics — a framer reads sheets faster in the convention they already know. Reference marks follow the crow's-foot convention (drawn at *half* the reference edge's width, so they cannot be mistaken for a full-width carpenter's triangle).
- **Shop-drawing generation is the fragile last mile** (August 2026 research): the highest density of complaint about existing timber software is not modeling but the drawing/BOM output — extensions breaking on version changes, cameras not squaring to the timber face, cut lists failing on non-axis-aligned parts. An experienced practitioner's summary of general CAD: *"the biggest problem is not drawing and modeling, but data manipulation — assign, extract, organize and export."* Treat this phase as the one that decides whether the workbench is genuinely useful, not as the reporting tail of Phase 1. Automate and refine TechDraw output rather than expecting the user to fix cameras by hand.
- **Degree of Difficulty per timber** (Tier 2, from the Guild-associated TF Rubies plugin): a joinery-complexity tag feeding labor estimating, since cost tracks difficulty rather than board feet. Cheap — an ordinary custom property the cut-list/estimating generator reads.
- **Share-with-viewers workflow**: FreeCAD already exports formats free 3D viewers can open, for clients and collaborators who need no edit access. Verify and document one working route rather than building anything.
- Evaluation item: per-bent driving **skeleton sketch** (native Sketcher) as the layout layer — the prior iteration's datum-line concept in native form.

## Phase 3 — Species & Grade Material Library
- Custom properties / Materials cards with NDS design values (Fb, Ft, Fv, Fc, Fc⊥, E, Emin), green vs. dry.
- Tier 2 compliant: data visible and round-trip-safe without the workbench.
- **Reference data system** (adopted): bundled read-only CSVs (species_properties from NDS, section_properties) with a user-override directory checked first at lookup; user rows visually distinct, base rows never edited in place.

## Phase 4 — Structural Checks (start simple, grow later)
1. **Load-path continuity** — every timber has a complete path to a bearing point; disconnected members flagged. Cheapest, most valuable early check.
2. Span-table checks (AWC tables via reference data system) and rule-of-thumb checks, advisory only.
3. Member stress checks: bending M ≤ Fb·S, shear V ≤ Fv·A·(2/3), deflection Δ ≤ L/360, driven by tributary-area load accumulation from **BuildingParameters** (roof material / snow / occupancy / wind presets, ASCE-derived, as a project-level parameter group).
4. FEM studies of individual joints (orthotropic properties via CalculiX) seeding a joint_capacities table (TFEC 1-2007 + FEA-derived).
5. Lightweight frame analysis with beam elements and semi-rigid joints from the capacities table; racking checks with per-bent brace contributions; bearing-point reaction schedule.
6. Load/capacity color overlay via native body ShapeColor (blue <30% → red >100%, grey unchecked); structural report with clickable findings.
Always labeled design guidance, not stamped engineering. External research (August 2026) supports keeping it advisory: framers appear to assume structural sign-off belongs to a licensed engineer, not the drawing tool, and a practitioner-developer of a comparable tool warns the hard part is not the geometry math but *"knowing how to apply it"* — species, joinery choice and load vary per project and cannot be safely auto-decided.

**Cross-joint interference checking** (adopted August 2026): the workbench already refuses a joint that will not fit its own timbers (`footprint_violations`), but nothing catches **two different joints colliding inside one timber** — a tie's tenon and a brace's tenon meeting secretly in a post, or a tenon running into a neighbouring joint's peg bore. This is layout work, and catching it here is enormously cheaper than catching it in the shop. Distinct from BIM clash detection, which the same research found framers do *not* ask for: that is an MEP-coordination concern (framing vs. ductwork), whereas this is joinery-vs-joinery inside a single stick, invisible until cut. Implementation is plausible without a full solid intersection: each joint's cut features are known per timber, so their bounding volumes in timber-local coordinates can be compared, with a true shape intersection only for candidates that overlap.

**Raising calculations — weights, tipping and balance points** (adopted August 2026; genuinely unclaimed ground): the Guild's own literature (Mullen, *Timber Framing* #67) describes computing a bent's tipping and balance points for rigging **by hand** — weight × height-of-midpoint summed over members, divided by total bent weight. Nothing in the surveyed software does this. BentWizard will hold both inputs already: real member geometry, and species density from Phase 3. Deliverables: per-timber and per-assembly weight, center of mass, and the tipping/balance points a crew needs to rig a raise. Depends on Phase 3; safety-relevant, so it ships with the same "design guidance, not stamped engineering" framing and shows its assumptions (green vs. dry density above all).

## Phase 5 — Building Composition (future, preserved from prior iteration)
Ground planes and bearing-point projection; auto-generated sill beams; split-level ground planes; frame openings (stairwells, dormers) generating headers/trimmers with load redistribution; wall-opening placeholders; chimney clearance zones; cantilever load paths; bent layout templates (King Post, Queen Post, Hammer Beam) as saved native layouts.

## Layout rules — Mill Rule primary; Square Rule / Line Rule as variants (reframed July 2026)

**The workbench's native convention is Mill Rule** (settled at the second-shakedown testing review, superseding the "Square rule" framing used through Phase 0). The drawings this design tool produces dimension an idealized straight, square timber — which is precisely a Mill Rule drawing. Square Rule's defining move — cutting the uneven surface of a rough-hewn stick down to the hypothetical ideal timber "inside" it — never appears on a drawing as a designed depth, because it depends on the unique variations of the specific log the framer selected for that position; it is field work with chalk line and spirit level, outside the model's scope. A framer working rough stock from our drawings does that reduction on site, finding the "milled" timber inside without planing the whole log. The housings we *do* draw are Mill Rule housings: **optional** bearing features that spread the load across the full beam width instead of relying on the tenon alone, with real designed depths — still supported and used, never required.

Earlier assessment (Phase 1, still accurate): the layout rule is **not** a foundation; it is a template-authoring convention riding on rule-neutral machinery. The rule is a *contract* between two templates — the timber template says where the reference planes are (section corner at the body origin → reference faces on the XZ/YZ origin planes), the joint template measures joinery from those planes — and the tools mostly clone whatever the author built rather than knowing geometry. The reference-face discipline is identical under Mill and Square Rule.

Corner-reference-baked pieces (shared by Mill and Square Rule), exhaustively:
1. `new_timber`'s section (reference corner at origin).
2. The `FACES` table + `_face_transform` in `apply_joint` — the only corner-reference-baked *tool code* (face complements `ddim - T`); ~20 isolated lines.
3. Two linter checks reference housing concepts (`joint-exceeds-footprint`, housing≤50% severing) — already no-op when a template has no `Housing_*` properties.
4. The `_Face1`/`_Face2` naming vocabulary.

Rule-agnostic (the bulk): the clone/rebuild machinery, `remove_joint`, junction-point coupling, end/hand placement transforms (frame-relative), the schema-driven dialog, and the **MateFrame** (a pure "these two frames coincide when engaged" declaration — zero knowledge of how either frame was derived; the reason engagement and future assembly are rule-blind by construction).

Cost:
- **Mill Rule = already built.** What exists today *is* Mill Rule: corner-referenced section, joint layout from reference faces and stick ends, housings as optional designed bearing features. No code owed.
- **Square Rule ≈ free.** Identical model geometry and drawings — the reduce-to-ideal housing is field work the framer performs on the rough stick, never a drawn dimension, so the model does not represent it. At most a label/note on drawings intended for rough stock.
- **Line Rule = moderate but contained.** Reference datum moves corner→center: a centered timber-template variant (centerline construction + half-widths; origin planes become centerplanes), a second `FACES` table (reflections about center, not corner complements, ~15 lines), and rule-aware linter vocabulary. Landing-frame expressions differ but are authored in the template, not tool code. No changes to clone/rebuild/mate/remove.

**Support the rules; do not force one** (settled August 2026). Mill Rule is one school among at least three named traditions, and there is a live practitioner debate: Line/Centerline Rule is described by some experienced framers as the older and more universal system — dominant in Asian framing for millennia, and better suited to round, tapered or irregular stock — with Mill Rule a comparatively recent post-sawmill convention whose modern dominance those framers actively regret. Framers working round or irregular stock are inside the target audience, so:

- **No rule is arbitrarily imposed.** Support as many as practical; a document may legitimately mix them, since `Rule` is a per-timber/per-template property.
- **There IS a default, and the user configures it.** Nobody should answer the question on every timber. The default lives in the workbench preferences, seeds each new project (see the New Project dialog in the front-end section), and remains changeable per project and per timber afterwards. "Not prescriptive" means *not deciding for the user*, not *interrogating the user*.
- Documentation, UI copy and any future marketing say **"a Mill Rule tool, with others supported"**, never "the timber framing method".

Structure when built: a single Tier 2 `Rule` property (`MillRule` default / `SquareRule` / `LineRule`) on timber Dims and joint templates; templates filter/label by it; New Timber offers the choice; `FACES` becomes rule-keyed; Apply Joint validates rule-compatibility; the linter branches face-vocabulary and housing checks on it. Caveat: a real Line Rule port wants an audit pass over every linter rule and the placement transforms to catch any corner-assumption under-counted above.

## Parametric layout — the grid drives, sticks derive (adopted July 2026)

**The problem.** Through the second shakedown, `Dims.Length` was authored and position was derived: type 12' for a tie and the solver puts the post wherever the joint frames land. That inverts the designer's intent. A tie with a 1" housing + 3" tenon at one end and a 1" housing + 11" through tenon at the other, set to 12'-0", yields 10'-8" clear inside — and a single `Span` value shared across bents with *different* joinery yields different actual dimensions bent to bent. Framers lay out on-center; the stick length is a consequence of the grid plus whatever joinery was chosen, never the other way round.

**The inversion.** The grid is authored; `Dims.Length` becomes an expression. No new object types, no new geometry — the project layer of the already-adopted layered parameter groups finally carrying real values.

- **Span basis is declared, not inferred.** A Tier 2 `Span_Basis` enum (`OnCenter` default / `ClearInside` / `BearingToBearing`) on the layout VarSet. **On-center is the native basis**: it is the only one stable under the edits actually made — changing a post section must change the clear span, not move the building.
- **Each joint publishes what it consumes.** `Stick_Allowance` = how much stick this joint adds *beyond the landing timber's grid line*, **measured along the entering timber's own axis** — `Tenon_Length + Housing_Depth - <landing ddim>/2`, where ddim is the landing timber's dimension along the joint normal (already tabulated: `FACES[n]["ddim"]`, Width on faces 2/4, Depth on 1/3). Negative means the stick stops short of the grid. Measuring along the member's own axis is what makes the arithmetic **angle-blind**, so diagonals need no separate mechanism.
- **The stick derives:** `Length = Bay_Span_OC + Stick_Allowance_A + Stick_Allowance_B`. Worked: 136" OC, 8" posts, 3"/11" tenons over 1" housings → allowances 0" and +8" → 144" stick, 128" (10'-8") clear. The clear dimension still varies between bents where posts or housings vary — correctly, and now visibly — while the **grid stays invariant by construction**, which is what the designer meant.

**The hard constraint: the layout VarSet must be a pure source.** The obvious arrangement — `Stick_Allowance` as a property *on the joint VarSet*, with the tie's `Length` summing the two — is impossible, and fails loudly (`cyclic reference to <doc>#JointVarSet001`). FreeCAD's dependency graph is **object-granular, not property-granular**, and every joint template already binds the joint to the entering timber's section (`Housing_Height = <<TDim_tie>>.Depth`, `Housing_Width = <<TDim_tie>>.Width`), so `jointVS → tieDims`; the moment `tieDims.Length` reads the joint back, the loop closes. No arrangement of properties on those two objects escapes it.

So the length arithmetic lives one level up, on a layout VarSet that **reads nothing that reads back from it**, and joints *consume* their tenon/housing from it rather than owning them. Corollaries: post sections are authored in the layout layer too (a layout VarSet that reads post Dims is also acyclic, but stops being acyclic the moment you want to drive those sections from it); the timber's `Length` expression must be written **last**, after both joints exist, since the second joint needs a resolved `Length` to place end B against. Nothing detects a cycle before FreeCAD refuses it — an advisory linter rule is owed.

**Verified** (headless shakedown, two posts + one tie, housed M&T both ends, plus the same chain hand-built in the GUI): stick derives to 144" from a 136" grid; `Bay_Span_OC` → 16'-0" moves the far post to 192.0000" solved on-center with the stick re-deriving to 200"; posts 8"→10" hold the grid at 192.0000" and re-cut the stick to 198" unaided; joints stay seated throughout (misfit ~1e-13 mm). In the GUI the bent updates on edit **with no explicit Solve** — editing the span touches the landing frames, which touches the Fixed joints, which pulls the assembly into the recompute. A manual solve is only needed after something moves *outside* the dependency graph, which is precisely what the Transform tool does.

**Diagonals — same mechanism, one level richer.** What changes is only the span term, and the insight is that **each diagonal family holds a different designer invariant, and it usually is not the span**:
- **Braces** — set out by run and rise on the two members, not by bay width. A 3'×3' brace stays 3'×3' when the bay widens; braces bind to `Brace_Run`/`Brace_Rise` and correctly ignore Span. Length `= sqrt(run² + rise²) + allowances`, angle `= atan2(rise, run)`.
- **Rafters** — the invariant is *pitch*: `Length = Run / cos(Pitch) + allowances`.
- **Struts** — an angle plus one anchored station.

The rule to build to: **the designer names the invariant; the template declares which one it holds; everything else derives.** Same shape as the `Handed` flag and the declared angle range — template self-describes, tools read it, no per-joint code. It lands directly on the adopted parametrically-angled-joints work, whose joint angle is already a VarSet property driving the landing frame, and `atan2` over layout values is a legal expression for it.

**Template authoring: the companion VarSet (verified July 2026).** `Stick_Allowance` *can* be authored in the joint template, via a second VarSet the template declares per joint — `Layout_J-<Kind>-<serial>`, kind-owner per the naming convention. It holds the length-consuming parameters (`Tenon_Length`, `Housing_Depth`) **authoritatively** plus the derived `Stick_Allowance`, and reads only the landing timber's Dims; the joint VarSet *consumes* tenon/housing from it. That direction is the whole trick — `tieDims.Length → companion → postDims` while `jointVS → companion` and `jointVS → tieDims`, so nothing the companion reads reads back. Verified end to end: stick 144" from a 136" grid, span→16' giving 200"/192.0000", posts 8→10" holding the grid at 192.0000", and — the edit that matters — changing the joint's tenon 11"→14" re-cutting the stick to 201" without moving the grid, joints seated throughout.

A second arrangement was tested and rejected: binding the junction *through* to the tie's section VarSet (so the joint never references the tie's Dims) also works, and lets the allowance live on the joint VarSet directly — but only when the section is group-bound. With a plain literal section, which is the default, there is nothing to bind through to and it is the same cycle. Not viable as the mechanism; not worth a second code path as an optimization.

Cost of the companion: `TemplateSpec` reads a second VarSet per joint; the generated Apply Joint dialog merges the two schemas (it is schema-driven, so a merge, not a rewrite); Duplicate and Remove Joint carry/clear the companion with its joint. Tree clutter is absorbed by the per-joint 3D handle (built July 2026), whose Group extension holds the joint VarSet already and takes the companion beside it — `joint_handle.handle_varset` picks the joint VarSet out by label, so a second child needs no change there. Linter (strict): the companion must be a pure source — flag anything it reads that reads back — which is the rule that stops the cycle being rebuilt.

**Two bases, O.C. and clear span** (built August 2026). Framers lay out both ways, so `Drive Length from Layout Distance` offers each: `Length = distance + Σ Stick_Allowance_OC` (centerline to centerline) or `+ Σ Stick_Allowance_FTF` (face to face). The clear-span form is the *simpler* one — `Stick_Allowance_FTF = Tenon_Length + Housing_Depth`, with the half-thickness terms cancelling exactly — and the companion publishes `Grid_Setback` (= ddim/2) as the conversion, since `OC = FTF + Σ Grid_Setback`. **The basis belongs to the distance, not the tool**: a variable is an on-center number or a clear-span number for good (suffixes `_Dist_OC` / `_Dist_FTF`), so there is no mode to set wrongly, and which allowance a driven `Length` sums *is* the basis — it reads back structurally, never from the name. The dialog shows O.C., clear span and the resulting stick together and live, so a mismatch is visible rather than silent. Clear span is offered only for a timber landing on exactly two timbers; one end or three has no face-to-face distance, while O.C. stays well defined. Removing a joint freezes only *its* allowance term at the resolved value, so the stick does not shrink by the tenon it just lost and drag the far post with it; stopping (or switching basis) offers to delete the orphaned distance, but only once nothing — including a sibling property referenced bare on the same VarSet — still consumes it.

**Owed, in order.** (1) *Done (August 2026)*: the companion-VarSet template support described above. (2) Apply Timber Joint / the Beam tool writing the timber's `Length` expression, opt-in per timber, with Remove Joint reverting the term to the resolved literal (mirroring how it already removes the Fixed assembly joint). (3) A derived `Clear_Inside` read-out per bay, so the dimension you do *not* drive stays visible, feeding a Phase 4 advisory check on bays diverging from typical. (4) Linter: expression-cycle advisory, and `Layout_`/project VarSet kinds in the naming rule (a hand-built `Layout_Main` currently trips `naming-convention`; `Project_Main` is already accepted).

## Framer-facing front end (adopted August 2026; expanded after the August planning session)

**Who it is for** (settled August 2026): individual DIY builders through small independent contractors — the people priced out of SketchUp subscriptions and nowhere near Dietrich's or Cadwork. Free and open-source, no subscription, genuinely useful rather than a demo. **It is a tool for designing frames, producing shop drawings and BOMs, and preparing for a build.** It is *not* aimed at non-technical clients driving it themselves: a few hours of learning is an acceptable price, weeks is not, and the user is assumed comfortable with a mouse and keyboard. Sharing with people who need no edit access goes through FreeCAD's existing export to formats free 3D viewers can open — a workflow to verify and document, not to build.

External research (August 2026) supports the shape of this: purpose-built timber software (Dietrich's, Cadwork) earns its adoption by being domain-native — users report productivity in hours to days — while general CAD plus a plugin does not, and FreeCAD is publicly characterised as steep-learning-curve. That is the headwind. The same research found the realistic win condition for trades software is usually **"good enough that a junior person can drive it for the senior framer"**, which is a more achievable bar than universal self-service and changes what the UI optimises for: legibility of *output* over hand-holding of *input*.

**Delivery: a dock panel, with the tree left available** (settled August 2026). The model tree stays for users who want it; the panel is the abstracted route for those who do not. A full workspace that hides FreeCAD's own UI is deferred — reconsidered only if the panel proves insufficient, since it is far more work and risks fighting the host application.

**Scale target:** eventually multi-story, multi-wing structures up to ~12 bents. Build and prove features at smaller scale first; a two-story house and a standard barn are the common cases to design against.

**Sequencing is a priority, not a rule** (settled August 2026, superseding "deliberately last"). Individual front-end features ship as soon as they are thought through and have a back end to support them. The reason for the change: through the July–August work, *both* GUI failures were affordance problems, not logic problems — a project VarSet invisible to every expression field, and a Stop Driving control present but buried below thirty rows of joint parameters. The technical layer has held up; the UI is where the failures are.

### Near-term (cheap, and pulled forward)

- **Variable → highlight everything that reads it**, directly or indirectly. The dependency graph is already in the document (`InList`/`OutList` plus the expression engine), so the transitive closure is a short recursive walk. Highest value per line on the list, and a debugging tool for the workbench itself. **Must use a transient view override or the selection mechanism** — writing `ShapeColor` would repaint the model permanently, which is Tier-1 data.
- **Toolbar icons**, so the toolbar stops growing linearly and commands can group into dropdowns.
- **Bulk-apply a project variable to selected members** — the existing "assign selected to group" item, which is what makes project variables feel structural rather than clerical (Adam's example: one `Floor01_Joist_Height` driving every first-floor joist's station).
- **Reference-face marks, face numbers and stick-end labels** in the 3D view, on the proven joint-handle ViewProvider machinery. **Match the crow's-foot convention**: framers draw the reference mark at *half* the width of the reference edge precisely so it cannot be confused with a full-width carpenter's triangle. Free to match, immediately legible to a framer. Face labelling/order must be **configurable**, not fixed — a competing SketchUp plugin added an option purely to mimic Dietrich's face order, which says drawing conventions are muscle memory, not cosmetics.

### Next (moderate, in Adam's priority order)

1. **3D selection → a panel showing parameters, state and relationships.** Select a timber or joint in the viewport and get its numbers, where each one lives, what drives it and what it drives, plus the tools that act on it. Feasible via a selection observer. **A 3D right-click context menu is not** — FreeCAD does not dispatch `setupContextMenu` for our objects from the viewport (upstream #14701/#11429/#12826), which is why joint operations live on the tree today. The panel *is* the workaround, and Adam judges it nearly as good.
2. **Generate a bent from a 2D sketch — one way only** (settled August 2026). The sketch is a starting surface, not a permanent source of truth: once it becomes 3D timbers and joints, editing happens in 3D through parameters, and there is no round-trip back into the sketch. If a fresh 2D layout is wanted later, TechDraw exports one that replaces the old. This is roughly a tenth of the cost of bidirectional sync, and the research found *no prior art* for a 2D-drives-3D generative pipeline in traditional framing — practitioners draw in 3D with a 2D plan beside them — so the expensive version is also the unproven one.
   - **Trace over an imported image or PDF** as a reference underlay. An estimator in the research wanted exactly this: start from the 2D plan set that already exists rather than abandoning it. FreeCAD can place an image plane natively; PDF import needs checking.
   - **Ground grid driven by project variables** for placing posts and spacing bays. FreeCAD has no native grid object, so it is built from datums or a sketch; the design question is whether the grid *drives* placement or only displays it.
   - **Isolate a bent for orthographic editing** (Adam, August 2026): hide the rest of the frame and square the view to one bent so it can be adjusted in plan/elevation without obstruction. Cheap, independently useful, and worth building before the sketch generator — it is most of what "work on one bent in 2D" actually means in practice.

3. **New Project dialog — the settings that carry through a build** (adopted August 2026). One place, at the start, for the decisions that would otherwise be re-answered on every timber: framing rule, species/grade/moisture, the standard timber sections this shop actually buys, trim allowance, peg stock, and the starting layout distances (bay spacing, story heights). **Skippable, and everything it sets stays editable afterwards** — per the non-prescriptive principle, it must not become a gate a user has to clear before modeling.

   **Two levels, and the distinction matters:**
   - *Workbench preferences* (FreeCAD's parameter store, a native preferences page) hold **the user's defaults** — their usual rule, their usual species, their mill's stock sizes. This is what stops the dialog being an interrogation.
   - *Project settings* are Tier-2 properties on a `Project_`/`Order_` VarSet in the document, seeded from those preferences at creation. The document owns its own answers, so it opens correctly on another machine with different preferences — and, being ordinary VarSet properties, they survive with the workbench uninstalled.

   Two things fall out of this that are worth having for their own sake. It is the natural moment to **create the project layout VarSet with real starting variables**, which removes the current requirement that a framer hand-author one before `Drive Length from Layout Distance` is usable at all. And **standard timber sections** turn New Timber's free numeric entry into a pick-from-stock list, which is both faster and closer to how someone orders timber; the section list should reuse the Phase 3 reference-data system (bundled CSVs plus a user-override directory) rather than inventing a second format.

### Later / bigger

- **Dimension displays in the 3D view, including which variable feeds each.** No native persistent 3D dimension object exists (the Measure tool is transient, TechDraw is 2D), so these are custom-drawn annotations — view-only and Tier-2 safe, but real work, and easy to make the viewport unreadable. Prototype one dimension type before committing.
- **Joint library previews.** A screenshot captured at save-as-template time is easy (`saveImage` on the active view); a small rotatable 3D preview means embedding a second viewport in a dialog — possible, harder, a later upgrade. The exploded view is nearly free either way: it is a placement offset on the mate half, which Preview Mated Joint already computes.
- **A workspace that hides FreeCAD's UI**, only if the dock panel proves insufficient.

### Principles

- **Do not be prescriptive.** Framers plan, design and build differently; provide multiple routes between stages and smooth transitions back and forth, rather than one blessed sequence.
- **Let the user pick which parameter they author.** The O.C./F.T.F./stick-length trio is the model: several related numbers, the user edits whichever suits, the rest update and stay visible. **The honest form of this is "choose the driver, the others derive"** — a FreeCAD property is either a literal or expression-bound, so true simultaneous bidirectionality would need a constraint solver sitting between the user and the model, which Tier 1 argues against. Make "choose the driver" a repeatable idiom instead.
- **Terminology stays governing:** timber-framing terms everywhere, "timber joint" never bare "joint", units deferring to the user's FreeCAD schema.
- **Name things after the work, not the implementation** — see CLAUDE.md's audience note, which this section exists to serve.

### The ergonomic debt this pays off

- **The two-VarSet juggle** (named at the companion-VarSet GUI test, August 2026): `Tenon_Length` and `Housing_Depth` moved to the companion and are no longer editable on the joint VarSet — writes there are silently overwritten. Technically forced, and a bad affordance; a front end that shows one "tenon length" field and writes it to the authoritative VarSet removes it entirely.
- Junction-bound parameters (`Housing_Height` ← the entering timber's Depth) have the same shape and the same fix.
- The layout layer's discipline (`Span_Basis`, the pure-source rule) is a *constraint on the model*, not something a user should hold in their head — the front end enforces it by construction.

Related items already logged, which this subsumes or feeds: the dock panel showing a timber's Dims when selected; the parameter-provenance UI; joint parameter presets.

## Open Items
- **Dihedral angle finder** (August 2026 research): pick two connected faces, get the angle between them. Praised in an existing timber plugin as directly useful for hip and valley joinery, where the trig is the normal pain point. Lands naturally alongside parametrically angled joints, and is a small, self-contained tool.
- **Wedged Half Dovetail needs a companion layout VarSet** (August 2026 testing): `Joint_HousedMT` declares one, so it drives lengths; `Joint_WedgedHalfDovetail` does not, so a timber entering it publishes no allowance and `Drive Length from Layout Distance` refuses. Mechanical work — add the companion, move its length-consuming parameters onto it authoritatively, author `Stick_Allowance_FTF`/`_OC` and `Grid_Setback` — but it needs a decision first on what "the joinery beyond the face" means for a dovetail, whose housing and tail depth are not the M&T's `Tenon_Length + Housing_Depth`. Every new template in the library inherits this requirement, so it is worth settling the pattern once here.
- **`Components` subgroup inside a timber Body** (August 2026 testing): frames, sketches, pockets and datums nested in one folder per Body so the tree reads as a list of timbers rather than a wall of features; the `TDim_` VarSet deliberately stays at Body level, where it is easy to find and edit. Feasibility is genuinely unproven and should be tested before it is planned: a `PartDesign::Body`'s `Group` carries its features in solid-chain order, and it is not established that 1.1.1 tolerates a Std Group among them without breaking `Tip`/`BaseFeature` resolution. It would also touch every place that walks `body.Group` — `dims_varset`, `TemplateSpec._base_members`, `joint_members`' closure, `duplicate` — each of which currently assumes features are direct children. Cheap to disprove with a headless spike; do that first.
- **Blank joint-template starters** (August 2026 testing): a "new joint template" file carrying the pieces every template needs — two pristine timber bodies with their Dims, landing and mate frames with `Frame_Role` set, a joint VarSet, a companion layout VarSet — and no joint geometry at all, so authoring a new joint starts from a valid skeleton rather than from a copy of an existing joint (finding #12's lesson, one level up). Probably several: reference-face vs. datum-centered (the Mill/Line Rule split), and end-to-face vs. end-to-end, since those differ in which origin plane the landing frame attaches to and therefore in what `end`/`face` placement means. Pairs naturally with Save-as-joint-template — the same validation serves both — and each starter must lint clean strict *and* advisory as its acceptance test.
- **Principal Post visibility** (from the second shakedown): the Principal timber should be visually distinguished in the 3D view and tree (beyond the stock red padlock on its GroundedJoint) — ties into the reference-face visualization item below. Also: Apply Timber Joint currently picks the anchor as Principal when it creates a new bent and announces it in a popup; a picker in the dialog (shown only when both timbers are loose) would let the user choose up front instead of regrounding via Assemble Timbers.
- New Timber dialog (expanded July 2026, testing notes):
  - **Expression fields** — *done (July 2026)*: each dimension row carries an fx toggle swapping the quantity spinbox for an expression field; `new_timber` accepts `"=<expression>"` per dimension and binds the Dims property via `setExpression` (bad or non-positive-resolving expressions rejected with no debris left in the document). The front door to the adopted layered-parameter-groups design — binding a post's Length to `<<PostDims_Balcony>>.Post_Height` is exactly "membership IS the binding"; edit the group VarSet later and every bound post moves.
  - **Copy-from-selected** — *done (July 2026)*: a "Copy from" dropdown (default "(new timber)"), preselected when a timber is selected at dialog open; prefills the name base with the serial bumped (`T-Post.Balcony.001` → `T-Post.Balcony.002`) and the dimensions — expressions included, so group bindings carry over. Discipline per finding #2: an expression mentioning the source body or its Dims is copied as the literal value instead — audit, don't blind-clone.
  - A Role picker (existing role bases in the document) feeding the auto-serial, and structured position pickers (Bent/Bay/Level) composing Position_Tag once bents exist (ties to the Phase 2 Role property); custom labels stay allowed, nudged by the advisory naming rule.
- Apply-Joint parameter bounds, phase 2 of: live min/max clamps on the dialog fields computed from the chosen timbers (the pre-flight refusal on resolved values exists); longer term, constraint relationships declared in the joint template itself (Tier 2 metadata) instead of name-keyed heuristics.
- Visual indication of the reference faces (Face 1/Face 2) on a timber in the 3D view.
- Joint parameter presets ("TieBeam01"): note this is the **type layer of the parameter groups design** (§4.9 instance → type → …) — a preset = a Group VarSet the instance binds to, giving persistent, editable-once presets rather than dialog memory. Near-term convenience: a "copy parameters from existing joint" picker in the Apply Joint dialog.
- **Primary/secondary timber roles per joint** (load-path based — the primary carries the weight of the secondary). Currently inferred structurally: the mate-frame carrier is the secondary (the piece that enters and is previewed as a ghost), the other role the primary. Formalize as explicit joint-template metadata (Tier 2) with a load direction — feeds Preview (which half ghosts into which), Assembly (which is grounded), and the Phase 4 load-path continuity check. The mate-frame heuristic is correct for M&T but should not stay the definition.
- **Preview Mated Joint UI**: the run-again-to-clear toggle works but is unintuitive. Wants a clearer affordance (a visible preview state / dedicated clear). Live parameter tracking is done (the ghost attaches to the landing frame via its body-local placement, following station/section/tenon-shape edits and seating wherever the primary body sits). Untracked, needing a refresh: edits that move the mate frame within the secondary — its Width or Depth (mate frame at Width/2, Depth/2) or Tenon_Length (its Z) — because the attachment offset to that frame is fixed at creation; and moving a body with the transform tool while the preview is up. **Complete fix (backlogged):** drive the ghost placement from a recompute hook (a small App::FeaturePython whose `execute` recomputes `engagement_placement` and writes the link placement, depending on the joint VarSet and both bodies) — covers every case, at the cost of a transient proxy class. Deferred as low-priority since a refresh is one toggle.
- Joint browsing at scale: the Remove Joint picker (and future joint dialogs) needs filtering by Bent/Bay/Timber once projects carry hundreds of joints — though the per-assembly `TimberJoints_<Bent>` groups now give the tree that structure for free. *Mostly answered by the per-joint handle (July 2026)*: the marker and the landing frame both preselect the picker, and the handle's context menu carries whole-joint operations. Still owed there: **re-place operations** — rotating a mortise onto a different face, moving a joint's station, changing hand, without removing and re-applying. That is the natural next action to register in `joint_handle.CONTEXT_ACTIONS` (the `Placement_Record` already stores what each role was applied with).
- Tree/panel ergonomics: a dock panel showing a timber's Dims VarSet when the timber is selected. The Dims VarSet now nests inside its Body (adopted with the naming overhaul — the earlier link-scope concern did not materialize: 1.1.1 accepts a VarSet as a Body member, and junction bindings into it recompute, save, and reload with no scope complaints headless; if the GUI's expression editor objects in practice, fall back to root-level Dims). (Also answers the setback-naming concern in docs/User Notes.txt: `_Face1`/`_Face2` qualifiers are the stable reference-face vocabulary, but which physical direction they mean per timber needs to be visible.)
- Full-depth tenon variant (tenon height = beam depth, fewer shoulder cuts): the island pocket breaks on coincident edges at exactly that limit (finding #14), so it is a separate library template built on removal-region sketches — per the finding #14 rule, boundary-touching kept profiles use removal regions, not islands.
- **Diminished housing template** (July 2026, testing notes): angled cuts on *perpendicular* intersections are pure template authoring — the clone machinery is geometry-blind; the slope is a line in the housing profile sketch, its parameters ordinary joint VarSet properties. Cost is a `library/` entry plus, at most, extending the rebuilder's feature vocabulary (planned-for: "extend alongside new templates"). Good second template to drive Save-as-joint-template. Not to be confused with parametrically *angled intersections* (Phase 1 bullet).
- Units: decided (Phase 1) — the workbench defers to the user's FreeCAD unit schema (e.g. "Building US" for fractional inches), never forcing imperial or metric. TechDraw dimension style details remain open.
- Feature label scheme within bodies: resolved — descriptive-first `<Descriptive>[.<TypeTag>].<Abbrev>.<serial>` (workflow doc §3), written by Apply Joint. Reworked July 2026 off the body-qualified form, which was unreadable in the tree once templates grew past two cuts; frame role moved to the Tier-2 `Frame_Role` property at the same time.
- Joint library location (folder vs. addon structure) — decide at Phase 1 start.
- Single document vs. multi-file for large frames — revisit after the test bent.

## Division of Labor
Adam: FreeCAD driving, joinery domain decisions, testing against real workflow. Claude: all Python development, FreeCAD API research, step-by-step instructions, file-inspection verification (against resolved global placements — findings #10).
